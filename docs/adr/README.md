# ADRs

All meaningful decisions are recorded here per `AGENTS.md` §4.4 and the schema in `docs/templates/adr.md`.

File naming: `S-<NNN>-ADR-<CCCC>-<title>.md`.

The bootstrap sprint S-000 produced no ADRs — the structural choices (no OCR, RLS-required, country-agnostic schema, pluggable parser, secrets via env) are mission-level constraints recorded directly in `AGENTS.md` §2.4. New decisions taken in subsequent sprints land here.

For the index, see `.agents/context/decisions.md`.
