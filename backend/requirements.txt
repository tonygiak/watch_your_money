fastapi==0.115.6
uvicorn[standard]==0.32.1
pydantic==2.10.3
requests==2.32.3
beautifulsoup4==4.12.3
supabase==2.10.0
python-dotenv==1.0.1
# tzdata: Python's `zoneinfo` falls back to the OS IANA db on POSIX systems
# (Linux, macOS) but on Windows there is no system db, so tests against
# `Europe/Athens` (per ADR-0005 §3) raise `ZoneInfoNotFoundError`. The
# `tzdata` package is the official PSF-maintained data-only shim recommended
# by the `zoneinfo` docs. Pinned here so `make check` is green on Windows
# dev machines too. Linux production runtimes ignore it (the system db wins).
# This dep was added as drift in S-004 (BLG-0013) and codified by ADR-0011
# in S-005 (`docs/adr/S-005-ADR-0011-Tzdata-codification.md`): standalone
# ADR, exact pin, PyPI already on the outbound allowlist, refresh on every
# `requirements.txt` audit.
tzdata==2024.2
# reportlab: pure-Python PDF generator chosen by ADR-0009
# (`docs/adr/S-005-ADR-0009-Pdf-export-pipeline.md`). Renders the
# `GET /export/business-expenses` PDF on the fly via `StreamingResponse`,
# never persisted server-side, never logged. Greek glyphs are rendered via
# the bundled DejaVuSans font; no system / native deps. Exact pin so
# Railway / Render builders don't pick up a surprise upgrade. Supply-chain
# review captured in ADR-0009 §Round 2 (co-signed by `agent-safety-officer`
# + `engineering-manager`); PyPI already on the outbound allowlist.
reportlab==4.2.5
# cryptography: asymmetric JWT verification (ES256 / RS256) against the
# Supabase JWKS endpoint per ADR-0015 (S-010). Replaces the HS256-only
# verifier from ADR-0002 §1. One new runtime dep — `PyJWT` was explicitly
# rejected in ADR-0015 Round 1 because it would have pulled `cryptography`
# transitively anyway (one dep, not two; one CVE-track to monitor).
# `agent-safety-officer` supply-chain review in ADR-0015 Round 1 (PyCA
# upstream, CVE history checked, no new outbound host — JWKS lives under
# `*.supabase.co` already on the allowlist). Exact pin so Railway / Render
# builders never pick up a surprise upgrade on an auth-critical path.
cryptography==45.0.1
